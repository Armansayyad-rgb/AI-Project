# Runtime dependencies for AI-Project.
# Pin the web UI stack to versions known to work together with Gradio 4.44.1.

# Core deep-learning stack (CPU-only is fine; CUDA is auto-detected at runtime).
torch>=2.7.1

# BPE tokenizer used by SmallLMV2 / SmallLM.
tokenizers>=0.22.2,<=0.23.1

# Numerical utilities used by models and tokenizers.
numpy>=2.4.4,<3.0

# Used by src/download_corpus.py to pull the WikiText corpus from HF Hub.
datasets>=5.0.0,<6.0

# Gradio 4.44.1-compatible web stack.
gradio==4.44.1
gradio_client==1.3.0
fastapi==0.115.3
pydantic==2.9.2
httpx==0.27.2
uvicorn==0.32.0
huggingface_hub==0.26.1

# Document upload pipeline.
PyPDF2>=3.0.0
python-docx>=1.0.0
